<%-include ('partials/_header.ejs')%>

<main class="container pt-2 text-center d-flex justify-content-center ">
    <section>
    <h1 class="display heading">Listado de platillos</h1>
    <p>A continuación, se va a estar presentando la lista de platillos. Espero que disfrute si comida.</p>
    </section>
    
</main>

<div class="container">
    <div class="col-md-5">
        <div class="card">
<input type="text" name="buscador" id="" placeholder="Buscar...">
        </div>
    </div>
</div>
<hr class="bg-black">

<div class="container">
    <div class="col-md-4">
        <div class="card">
           <div class="card-body bg-dark">
            <form action="/add" method="post" class="bg-dark">
                <div class="form-group row mt-2">
                    <input type="text" name="platillo" placeholder="Ingresar el platillo" class="form-control">
                </div>
                <div class="form-group row mt-2">
                    <input type="text" name="precio" placeholder="Ingresar el costo" class="form-control">
                </div>
                <button type="submit" class="btn btn-primary">guardar</button>
    
            </form>
           </div>
        </div>
    </div>





    <div class="row mt-5 card-body">
        <div class="col-md-8 card-body">
            <table class="table table-dark">
                <thead>
                  <tr>
                        <th scope="col">#</th>
                        <th scope="col">Platillo</th>
                        <th scope="col">Precio</th>
                        <th scope="col">Acciones</th>   
                    
                  </tr>
                </thead>
                <tbody>
                    <% if (data) { %>
                        <% for(var i=0; i < data.length; i++){ %>
                           <tr>
                               <td><%= (i+1) %></td>
                               <td><%= data[i].platillo %></td>
                               <td><%= data[i].precio %></td>

                               <td>
                                <a href="/delete/<%= data[i].id %>" class="btn btn-danger">Eliminar</a>
                                <a href="/update/<%= data[i].id %>" class="btn btn-info">Editar</a>
                               </td>
                           </tr>

                           <% } %>
                           <% } %>
                    


                </tbody>
              </table>
        </div>
    </div>



    
</div>







<%-include ('partials/_footer.ejs') %>